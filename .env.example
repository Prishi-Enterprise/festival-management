# Use local Supabase only. Run npm run db:env after npm run db:start.
# Never put the hosted production URL or service-role keys here.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_APP_ENV=dev
APP_URL=http://localhost:3000
